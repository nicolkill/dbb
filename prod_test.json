{
  "ui": {
    "title": "example title"
  },
  "schemas": [
    {
      "name": "order_product",
      "fields": {
        "order_id": "string",
        "product_id": "string"
      }
    },
    {
      "name": "orders",
      "fields": {
        "user_id": "string",
        "delivery_time": "datetime"
      }
    },
    {
      "name": "users",
      "fields": {
        "email": "string",
        "name": "string",
        "last_name": "string",
        "age": "number",
        "male": "boolean",
        "birth": "datetime",
        "flags": ["string"],
        "sku": "string"
      },
      "generate": {
        "sku": "$str(4)$-$num(4)$-$sym(4)$-$any(4)$"
      }
    },
    {
      "name": "products",
      "fields": {
        "name": "string",
        "expiration": "datetime"
      }
    }
  ]
}
